import { title } from "@/components/primitives";

export default function DocsPage() {
  return (
    <div>
      <h1 className={title()}>Fill this out with info for each of the tools</h1>
    </div>
  );
}
